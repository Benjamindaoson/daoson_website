---
layout: null
permalink: /feed-zh.xml
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh-CN">
  <title>{{ site.title | xml_escape }} · 中文</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link href="{{ '/feed-zh.xml' | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" hreflang="zh" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/feed-zh.xml' | absolute_url | xml_escape }}</id>
  <author>
    <name>{{ site.author | xml_escape }}</name>
    {% if site.email %}<email>{{ site.email }}</email>{% endif %}
  </author>

  {%- assign zh_posts = site.posts | where_exp: "p", "p.lang == 'zh' or p.lang == nil" -%}
  {%- for post in zh_posts limit: 30 -%}
    <entry>
      <title>{{ post.title | xml_escape }}</title>
      <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" />
      <published>{{ post.date | date_to_xmlschema }}</published>
      <updated>{{ post.date | date_to_xmlschema }}</updated>
      <id>{{ post.url | absolute_url | xml_escape }}</id>
      <author>
        <name>{{ post.author | default: site.author | xml_escape }}</name>
      </author>
      {% if post.description %}<summary>{{ post.description | xml_escape }}</summary>{% endif %}
      {% for tag in post.tags %}<category term="{{ tag | xml_escape }}" />{% endfor %}
      <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}">
        {{ post.content | strip | xml_escape }}
      </content>
    </entry>
  {%- endfor -%}
</feed>
